# image-quality-enhancer

Enhance or upscale at least one existing input image using traditional processing and optional AI super-resolution. Never use for text-to-image or generating a new image from a prompt.

The source launcher uses the repository's canonical productivity runtime. The release builder vendors the required runtime into each standalone ZIP.
